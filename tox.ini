[tox]
envlist = coverage-clean,py35,coverage-report


[testenv]
deps =
    -rrequirements-test.txt
commands = coverage run manage.py test


[testenv:coverage-clean]
deps = coverage
skip_install = true
commands = coverage erase


[testenv:coverage-report]
deps = coverage
skip_install = true
commands =
    coverage report
    coverage html
